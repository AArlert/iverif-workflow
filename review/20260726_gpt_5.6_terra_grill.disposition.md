# 处置记录：20260726_gpt_5.6_terra_grill

- 审查来源：gpt 5.6（外部模型审查），原文见同名 `.md`，逐字入库
- 收到：2026-07-26 · 处置：2026-07-28 · 处置版本：0.3.1
- 处置时 adopter 实况（只读勘察 pulp_axi_xbar_copilot，未触碰其文件）：
  框架 pin 0.3.0，copilot profile；M0/M1 已 signoff，M2 中段（testplan
  8/10 ✅，约 7 小时未提交工作）；**功能覆盖率（F-M2-08）正在飞行中**——
  code coverage 在收（六类 `-cm`），covergroup 未写、URG 注释中、
  `coverage-waivers.md` 不存在；`doc/fw-feedback.md` FB-8 尚 open；
  formal 使用量为零。

## 处置原则

每条建议必须落入且只落入一格：**已覆盖**（给出出处）／**触发即建**
（`docs/deferred.md` 行 + 具体触发器）／**拒绝**（书面理由入正典）。
不允许存在"听起来有道理但无处安放"的建议——这与 deferred 台账管机制
的规矩是同一条。

## 逐条处置

| # | 审查观点 | 判定 | 落点 |
|---|---|---|---|
| 风险1 | 证据偏"单次 PASS"，缺功能 closure 的系统性证明 | 部分成立 | 覆盖闭环机械层已有登记（xcov 自动抓取行），触发器 = 第一次覆盖驱动的 milestone signoff，随隔壁 M2 将至；rubric 第 4 条（覆盖闭合≠风险闭合）已在 |
| 风险2 / 建议1 | spec 只防篡改；要 requirements→evidence 显式追踪 + 断链检查 | **采纳，全篇最值钱** | `docs/deferred.md` chain-audit 行（原 `spec_ref`/`coverage_delta` 硬校验行扩写）：全图查断 spec→场景→checker→覆盖→evidence，同一触发器。SPEC-x.y 锚点、feature-matrix、ghost-ref 检查已在，审查对 `make chain` "只显示不查断"的批评准确 |
| 风险3 / 建议4 | 小卡走全流程拖吞吐；要 L0–L3 风险分级 | 半成立 | "每张小卡都 ARCH→REV→DE→DV→REV"系夸大——dispatch 矩阵本就按卡型裁剪（DE fix 卡不过 arch）。分级路径登记 deferred，触发器 = 低风险卡流程开销明显压过工作本身、记录在案两次 |
| 风险4 / 建议3 | RCA 缺波形/签名的结构化工具接口 | 部分已有；其余出仓 | xdebug/xcov/xsva 已绑进 dispatch 卡与 FL schema（first_anomaly 要求 xdebug 定位）。错误签名聚类、transaction timeline 摘要属 **xverif 工具侧**路线图；工具存在后由框架绑进卡，CHANGELOG 0.3.1 记录此路由 |
| 风险5 | 隔离过严丢上下文；建议允许"临时协作、结论落证据" | **拒绝** | 理由入正典：`docs/profiles.md` "Why handoffs are files, not conversation"——该通道正是共模防火墙要堵的洞；重复挖波形的代价由 dispatch Q1 强制 grep 档案 + Q6 留痕纪律对冲 |
| 风险6 | 工具适配应 profile 化，别在一个 fragment 堆例外 | 为时过早 | 88 行的 `vcs-2018.mk` 以版本命名，本身就是 profile 边界。deferred 行登记，触发器 = 第二个工具/版本出现 |
| 风险7 | 缺"环境健康"独立概念 | 半成立 | TOOL_ENV 是 taxonomy 五类之首、诊断成本序第一位——事后归类已分离。缺的事前 preflight 登记为 `make env-check` deferred 行，触发器 = 第二次被环境卡死（第一次：FB-2 license 占位符，2026-07-27） |
| 建议2 | 覆盖闭环 Agent（不给签核权） | 时机未到 | 先让机械层过一个真实 M2（隔壁正在做），再决定 agent 化卡型；agent 化决策等隔壁 M2 经验数据 |
| 建议5 | 失败症状指纹 + 案例检索 | 触发即建 | deferred 行：FL schema 指纹字段（assertion 名/组件/first-anomaly 信号/工具错误码）+ 检索。触发器 = grep 检索第一次漏掉事后证明存在的同类 FL；当前最大仓库 15 个 FL，grep 尺度够用 |
| 建议6 | formal 纳入同一证据模型 | 方向对、触发即建 | deferred 行：formal evidence record schema 入 signoff rubric。触发器 = 任一项目第一次真实 formal run（现状：零 formal，VM 上有无 formal 工具未确认） |

## 审查的系统性盲区（记录在案）

审查把框架当产品路线图评，未察觉 `docs/deferred.md` 的"无痛点不建设施"
治理规则——其 6 个发展方向中 3 个已有登记行、2 个触发器恰被隔壁 M2 逼近。
其余判断（证据链/隔离/假绿工程细节的价值定位，"governance vs EDA
intelligence 叠加论"）准确，采信。

## 待隔壁数据（不打扰，后台只读监视中）

1. **FB-8 起头的第二轮回流批次**（`docs.py` NEXT_PHRASES 硬编码
   "dispatch DE card"，对 vendored-DUT 项目不成立）——等隔壁收口后收。
2. **M2 覆盖闭合实操**给 chain-audit 与 xcov 自动抓取两行触发定案：
   xcov 在 Verdi 2018 上输出是否稳定、waiver 流程首次真实走通的形态。

监视信号：M2 signoff 落盘 / 新 commit / `doc/fw-feedback.md` 变更。
