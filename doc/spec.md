# Spec

<!-- Sha256-pinned via `make check` / `scripts/docs.py --pin-spec`, which
     writes the digest to doc/spec.sha256. Expectations flow from this file
     only — never from the DUT under test. -->

## Example section

SPEC-1.1 the DUT shall (describe the behavior this clause pins down).

## Change record

| date | section | change |
| --- | --- | --- |
